# Apify API Configuration
APIFY_API_TOKEN=apifykey

# Note: Webhook URL is now configured directly in the Streamlit interface
# You can optionally set it here for convenience, but it's not required

# Optional: Default webhook URL (can be overridden in the interface)
# WEBHOOK_URL=https://your-webhook-endpoint.com/data

# Example webhook URLs:
# https://hooks.zapier.com/hooks/catch/123456/abcdef/
# https://maker.ifttt.com/trigger/event_name/with/key/your_key
# https://your-api.com/webhook/google-maps-data
